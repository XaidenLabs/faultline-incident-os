# Agent Trajectories

## Required artifacts

- `artifacts/trajectories/featured-replay.jsonl`: credential-free representative replay.
- `artifacts/trajectories/inc-2481-live.jsonl`: generated by a live model run.

## Reading a trajectory

Each line is one append-only JSON event. Relevant fields include:

- `sequence`: stable event order;
- `at`: UTC timestamp;
- `actor` or `type`: investigator, verifier, tool, model input or model output;
- `action`: selected reasoning or tool step;
- `arguments`: exact tool arguments;
- `output`: exact bounded tool response;
- `usage`: provider-reported token usage when available.

## Agent instructions

The complete instructions are versioned in `core/prompts.mjs`. They require competing hypotheses, evidence citations, allowed actions, human approval and explicit uncertainty.

## Human checkpoints

The representative workflow contains one mandatory checkpoint: approval of the sandbox recovery replay. Production execution is not implemented.

## Development-agent disclosure

Codex was used as the coding agent during the hackathon to research the problem, inspect the official PDF, create the implementation, run tests and review the product. The submission should include an exported representative development trajectory or a link supplied through the competition platform if required by the final upload form.

